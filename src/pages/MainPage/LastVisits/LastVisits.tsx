import classes from './LastVisits.module.scss'

const LastVisits = () => {
    return (
        <div className={classes.LastVisits}>
            LastVisits
        </div>
    );
};

export default LastVisits;